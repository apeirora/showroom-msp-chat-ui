{{- if and (not .Values.kcpKubeconfig.existingSecret) .Values.kcpKubeconfig.adminContent }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.kcpKubeconfig.secretName }}
  namespace: {{ include "chat-ui-msp-app.syncAgentNamespace" . }}
type: Opaque
stringData:
  kubeconfig: |
{{ include "chat-ui-msp-app.rewriteKubeconfig" . | indent 4 }}
{{- end }}
